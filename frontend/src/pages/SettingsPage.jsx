import PlaceholderPage from '../components/PlaceholderPage.jsx';

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Team membership, notification preferences, and AI provider configuration."
      phase="Phase 2 (authentication) + Phase 9 (DevOps)"
    />
  );
}
