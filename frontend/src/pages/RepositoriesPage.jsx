import PlaceholderPage from '../components/PlaceholderPage.jsx';

export default function RepositoriesPage() {
  return (
    <PlaceholderPage
      title="Repositories"
      description="Connect and browse GitHub repositories, trigger indexing, and track status."
      phase="Phase 3 (GitHub integration) + Phase 4 (ingestion)"
    />
  );
}
