import PlaceholderPage from '../components/PlaceholderPage.jsx';

export default function RepositoryDetailPage() {
  return (
    <PlaceholderPage
      title="Repository overview"
      description="Language breakdown, contributors, indexing status, and recent activity for a single repository."
      phase="Phase 3 (GitHub integration)"
    />
  );
}
