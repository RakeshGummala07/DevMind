import PlaceholderPage from '../components/PlaceholderPage.jsx';

export default function DocumentationPage() {
  return (
    <PlaceholderPage
      title="Documentation"
      description="Generate and regenerate README, architecture, API, and onboarding docs grounded in the indexed repository."
      phase="Phase 7 (documentation generation)"
    />
  );
}
