import PlaceholderPage from '../components/PlaceholderPage.jsx';

export default function CodeSearchPage() {
  return (
    <PlaceholderPage
      title="Semantic code search"
      description="Ask where something lives in the codebase and get ranked results with file, class, method, and relevance."
      phase="Phase 5 (AI: semantic search)"
    />
  );
}
