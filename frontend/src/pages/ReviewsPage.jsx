import PlaceholderPage from '../components/PlaceholderPage.jsx';

export default function ReviewsPage() {
  return (
    <PlaceholderPage
      title="Reviews"
      description="AI pull-request reviews across every connected repository, grouped by severity and category."
      phase="Phase 6 (AI PR review)"
    />
  );
}
